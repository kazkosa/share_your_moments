= render "devise/shared/nav"
.post-main
  .post-main__new_inner
    .post-main__new_inner__contents
      / Select an image
      .field.newpost_image
        .image_box_for_edit
          .image_box_inner
            = image_tag @post.image.url ,id:"current_image_for_edit", class: "filter-#{@post.image_option}"
        .input_post_wrapper
          .user_box
            .user_box_wrapper
              - if @post.user.avatar.attached?
                .avatar_box
                  .avatar_box_inner
                    = image_tag @post.user.avatar ,class:"avatar_image" 
              .user_name_box
                = @post.user.name
          .article_and_comment_box
            .article_box
              .article_box__title
                = @post.title
              .article_box__content= @post.content
              .article_box__timestamp
                = @post.created_at.strftime("%Y/%m/%d %H:%M")
              -if @post.location
                .article_box__location
                  Photo location:
                  %span.swithmap#map_confirm
                    = @post.location
                = hidden_field_tag "location", @post.location, id:"address"
                .article_box__location_map
                  .map#map
            
    
            -# - if @comments#あとで復活
            - if @post#あとで消す
              .comment-list
                -# - @comments.each do |comment|#あとで復活
                -if  @post#あとで消す
                  .user_box
                    .user_box_wrapper
                      - if @post.user.avatar.attached?
                        .avatar_box
                          .avatar_box_inner
                            -# = image_tag comment.user.avatar ,class:"avatar_image"#あとで復活
                            = image_tag @post.user.avatar ,class:"avatar_image" #あとでけす
                      .comment_box_main
                        .comment_box_main__name
                          -# = comment.user.name あとで復活
                          = current_user.name#あとでけす
                        .comment_box_main__text
                          -# =comment.message  あとで復活
                          Great 
                        .comment_box_main__timestamp
                          -# =comment.created_at.strftime("%Y/%m/%d %H:%M")#あとで復活
                          =@post.created_at.strftime("%Y/%m/%d %H:%M")#あとでけす
                -if  @post#あとで消す　
                  .user_box
                    .user_box_wrapper
                      - if @post.user.avatar.attached?
                        .avatar_box
                          .avatar_box_inner
                            -# = image_tag comment.user.avatar ,class:"avatar_image"#あとで復活
                            = image_tag @post.user.avatar ,class:"avatar_image" #あとでけす
                      .comment_box_main
                        .comment_box_main__name
                          -# = comment.user.name あとで復活
                          = current_user.name#あとでけす
                        .comment_box_main__text
                          -# =comment.message  あとで復活
                          Great 
                        .comment_box_main__timestamp
                          -# =comment.created_at.strftime("%Y/%m/%d %H:%M")#あとで復活
                          =@post.created_at.strftime("%Y/%m/%d %H:%M") #あとで消す
                -if  @post#あとで消す　
                  .user_box
                    .user_box_wrapper
                      - if @post.user.avatar.attached?
                        .avatar_box
                          .avatar_box_inner
                            -# = image_tag comment.user.avatar ,class:"avatar_image"#あとで復活
                            = image_tag @post.user.avatar ,class:"avatar_image" #あとでけす
                      .comment_box_main
                        .comment_box_main__name
                          -# = comment.user.name あとで復活
                          = current_user.name#あとでけす
                        .comment_box_main__text
                          -# =comment.message  あとで復活
                          Great 
                        .comment_box_main__timestamp
                          -# =comment.created_at.strftime("%Y/%m/%d %H:%M")#あとで復活
                          =@post.created_at.strftime("%Y/%m/%d %H:%M") #あとで消す
          .evaluate_box

          .comment_form
            = form_for [@post,@comment] do |f|
              .field.entercomment
                = f.text_area :text, autofocus: true, placeholder: "Message", required: true, class: "message__content"
              .actions.commentbtn
                = f.submit "Send", class:"btn btn-secondary", id:"comment_post"
      - if user_signed_in? && current_user.id == @post.user_id
        /each switches to state transition
        .select_step
          =link_to "Edit", edit_post_path(@post), class:"btn_map submit btn btn-outline-primary"
          %button.btn.btn-outline-primary{data: { toggle:"modal", target:"#exampleModal" } } Delete

<!-- Modal -->
.modal.fade#exampleModal{tabindex:"-1",role:"dialog", aria: { labelledby:"exampleModalLabel", hidden:"true"}}
  .modal-dialog{role:"document"}
    .modal-content
      .modal-header
        %h5.modal-title#exampleModalLabel Confirmation
        %button.close{data:{dismiss:"moddal"},aria:{label:"Close"}}
          %span{aria:{hidden:"true"}} &times;
      .modal-body Are you sure you want to delete this thread?
      .modal-footer
        %button.btn.btn-secondary{data:{dismiss:"modal"}} Close
        =link_to "Delete", "/posts/#{@post.id}",method: :delete, class:"btn_map submit btn btn-outline-primary"